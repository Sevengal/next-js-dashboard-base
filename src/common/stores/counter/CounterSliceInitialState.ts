import CounterSliceState from '@custom-types/stores/counter/CounterSliceState';

const CounterSliceInitialState: CounterSliceState = {
  counter: 0,
  showCounter: false,
};

export default CounterSliceInitialState;
